# Roadmap: SlideShow — приложение слайд-шоу для Android

## Стек
- Kotlin + Jetpack Compose (Material 3)
- minSdk 24 (Android 7.0+), targetSdk ~35
- Хранение картинок: только через Storage Access Framework (SAF), без разрешений на хранилище
- Адаптивность: планшеты больших размеров + телефоны (Window Size Classes)
- Форматы: ПРИНИМАЕМ ЛЮБЫЕ форматы изображений — JPEG, PNG, WebP, GIF, BMP, SVG, AVIF и HEIF/HEIC (от Apple). ВИДЕО НЕ ПОДДЕРЖИВАЕМ.

## Требования
- [x] R1. Поддержка любых расширений картинок (включая HEIF/HEIC), показ гарантирован
- [x] R2. Исключить видео из выбора и показа

## Шаги

- [x] 0. Каркас проекта: Gradle (Kotlin DSL, Version Catalog), Activity + Compose, навигация
- [x] 1. Экран выбора картинок:
  - [x] 1.1. Выбор из галереи через MediaStore Photo Picker / SAF
  - [x] 1.2. Выбор с флэшки через SAF (ACTION_OPEN_DOCUMENT_TREE), рекурсивный обход папок
  - [x] 1.3. Сохранение прав доступа URI (takePersistableUriPermission) + расшифровка tree URI
  - [x] 1.4. Список выбранных картинок (удалить/добавить)
  - [x] 1.5. Фильтрация: только изображения по MIME (image/*) — HEIF/HEIC/WebP/AVIF и др., БЕЗ ВИДЕО (R1, R2)
  - [x] 1.6. URI из системного Photo Picker (Галерея) теряют доступ после перезапуска приложения — исправлено: файлы без persistable-гранта копируются во внутреннее хранилище
- [x] 2. Экран слайд-шоу:
  - [x] 2.1. Полноэкранный просмотр по одному
  - [x] 2.2. Загрузка изображений через Coil (лениво, по мере показа) — декодирует все форматы, включая HEIF/HEIC на устройстве (R1)
  - [x] 2.3. Таймер смены картинок (настраиваемая скорость)
  - [x] 2.4. Режим «последовательно» (по списку)
  - [x] 2.5. Режим «перемешивание» (shuffle, без повторов пока не пройдём все)
  - [x] 2.6. Переходы между фото: 5 настраиваемых вариантов (без/кроссфейд/сдвиг/масштаб/флип), через AnimatedContent
- [x] 3. Настройки:
  - [x] 3.1. Скорость показа (задержка между картинками)
  - [x] 3.2. Порядок показа: последовательно / перемешивание
  - [x] 3.3. Тема: светлая / тёмная / системная (по умолчанию системная), через DataStore + Material3 dynamicColor
- [x] 4. Адаптивность для планшетов:
  - [x] 4.1. Window Size Classes
  - [ ] 4.2. (опционально) подписи/счётчик на больших экранах
- [x] 5. Полировка:
  - [x] 5.1. Сохранение состояния между запусками
  - [x] 5.2. Обработка поворота экрана
  - [x] 5.3. (опционально) «экран всегда включён» во время показа
  - [x] 5.4. (опционально) Скрытие системных панелей (навигационная полоска) во время показа (immersive), восстановление при выходе
- [ ] 6. Сборка и проверка:
  - [x] 6.1. debug build + установка на устройство/эмулятор
  - [x] 6.2. Проверка на телефоне и планшете
  - [x] 6.3. Подписанный release APK (R8/shrink) — версия 1.1 (versionCode 2), готов к RuStore
- [x] 7. Иконка приложения: светлая и тёмная (PNG от пользователя), светлая установлена как foreground adaptive-иконки
- [x] 8. (bugfix) Ревизия кода: позиция при shuffle/удалении, атомарная запись копий, лимит обхода с логом, подпись release без падения, GIF-анимация (API 28+)
- [x] 8.1. (bugfix, аудит) Исправления по результатам полной проверки: атомарная синхронизация таймера (гонка start/cancel), BFS-обход дерева вместо рекурсии/материализации, persistable-грант для tree URI перенесён в репозиторий (убран дубль из UI), popUpTo для стека навигации, explicit UTF-8 для SHA-256, `by lazy` вместо lateinit, согласование палитры тёмной темы, контрастная кнопка удаления, лог при неудачном delete, валидация URI при загрузке, удаление мёртвого ресурса иконки. Debug + R8 release собраны.
- [x] 9. In-App Updates (RuStore SDK Update 6.0.0): отложенное обновление (FLEXIBLE). При запуске проверяется наличие обновления в RuStore; если доступно — показывается UI-диалог SDK, после скачивания обновление устанавливается автоматически (completeUpdate). Зависимость `ru.rustore.sdk:appupdate:6.0.0`, maven-репозиторий VK, ProGuard-правила. Debug + R8 release собраны.
- [x] 10. UX-полировка по замечаниям:
  - [x] 10.1. Исправлен возврат из слайд-шоу: убран `inclusive = true` в `popUpTo` — кнопка «Назад» (стрелка) и системная кнопка теперь возвращают на экран выбора, а не сворачивают/ничего не делают
  - [x] 10.2. Добавлен `BackHandler` в экран слайд-шоу — системная кнопка «Назад» Android теперь делает шаг назад (возврат в приложение)
  - [x] 10.3. Grid-раскладка фото на всех экранах (телефон: 110dp ячейки, ~3 фото/строка; планшет: 180dp) — вместо одной картинки на строку
  - [x] 10.4. Helper-подсказки под кнопками «Галерея» и «Флэшка» (множественный выбор / авто-добавление всех изображений)
  - [x] 10.5. Красивый вид превью на главной: закруглённые углы (12dp, clip) у фото, уменьшенный аккуратный крестик (28dp круг + 16dp иконка) вместо большого
  - [x] 10.6. Крестик появляется только при нажатии на фото (как в галерее телефона): тап выделяет фото (рамка + подсветка) и показывает крестик, повторный тап снимает выделение; лёгкая тень между плитками (3dp в покое, 8dp при выборе)
  - [x] 10.7. Множественный выбор: можно выделить несколько фото сразу (Set<Uri>), у каждого свой крестик; крестик уменьшен и без серого кружка — просто белый крестик с тёмной тенью-контуром (виден и на светлых, и на тёмных фото), фиолетовая рамка выделения сохранена
  - [x] 10.8. Удаление по выбору: тап — выделение/снятие, крестик — удалить одно, корзина в top bar (появляется при выделении) — удалить выбранные, отдельная текстовая кнопка «Удалить всё» — очистить весь список
  - [x] 10.9. Статичная панель управления над списком (всегда видна при скролле): слева корзина «удалить выбранные» (при выделении), справа «Удалить всё» — не нужно возвращаться наверх после выбора разных фото; надёжное снятие выделения повторным тапом через clickable вместо pointerInput
  - [x] 10.10. Скролл фото: кнопки «Галерея»/«Флэшка» — первый item grid'а, уезжают вверх при скролле (панель управления и кнопка «Запустить слайд-шоу» остаются закреплёнными); кнопка «Запустить слайд-шоу» уменьшена (200×56dp, radius 28) и украшена цветной тенью
  - [x] 10.11. (bugfix) Кнопки «Галерея»/«Флэшка» больше не исчезают при «Удалить всё»: picker'ы всегда показаны первым item grid'а, при пустом списке выводится текст «Нет выбранных изображений»
  - [x] 10.12. Адаптивная кнопка «Запустить слайд-шоу»: на широких экранах (планшеты/горизонтальный) растягивается на половину ширины и увеличивается (72dp высота, крупный шрифт/иконка, радиус 36) — удобная мишень для пальца; на телефонах остаётся компактной (200×56dp)
  - [x] 10.13. Счётчик выделенных фото рядом с кнопкой «Удалить выбранные»: круглый badge «N/M» (выбрано/всего)
  - [x] 10.14. Переименование приложения: «Слайд-шоу» → «Слайд на Всё» (название приложения и заголовок главного экрана)
- [x] 11. Оптимизация загрузки папки с флешки (500+ фото):
  - [x] 11.1. Стриминг обхода дерева порциями (~40 фото) — список наполняется постепенно, устранено «зависание» 5–10 сек с чёрным экраном
  - [x] 11.2. Индикатор загрузки «Загружается… N фото» на время обхода
  - [x] 11.3. Даунскейл превью-плиток grid до размера ячейки (ImageRequest.size) — декод в 10–30 раз дешевле
  - [x] 11.4. Явные MemoryCache (25%) + DiskCache в глобальном ImageLoader — повторные показы не читают флешку заново
  - [x] 11.5. Даунскейл кадров слайд-шоу до разрешения экрана
  - [x] 11.6. Prefetch следующего кадра слайд-шоу — «не отрисованные» фото не выпадают из показа
  - [x] 11.7. (мелочи) getUris() один раз в SlideshowViewModel; валидация read-доступа по корню дерева (1 проверка на дерево вместо 1000+ binder-запросов)
  - [x] 11.8. Слайд-шоу больше не показывает «чёрные экраны» неготовых фото: таймер не листает вперёд, пока текущий кадр не отрисуется (лимит 4 сек на битый файл), буфер предзагрузки расширен до 3 следующих кадров, иконка ошибки отключена
  - [x] 11.9. Перескок через неготовые кадры: таймер хранит набор уже отрисованных URI, и при встрече с неготовым кадром показывает ближайший следующий ОТРИСОВАННЫЙ кадр (не чёрный экран); буфер предзагрузки — 5 кадров, каждый помечается готовым по факту загрузки
  - [x] 11.10. Устранён источник чёрных «кадров» между показами: «готовыми» помечаются ТОЛЬКО кадры с успешным execute (result.drawable != null), иначе битый файл показывался чёрным error-слотом; буфер предзагрузки расширен до 10 кадров; перескок неготового кадра сокращён до 80 мс
- [x] 12. Исправления по код-ревью (согласованы с пользователем по пунктам MID-1, LOW-2…LOW-6, INFO-7, INFO-8):
  - [x] 12.1. Helper-подсказки под кнопками «Галерея»/«Флэшка» (были отмечены как сделанные в 10.4, но потеряны при переводе кнопок в первый item grid): маленький текст 12sp под кнопкой внутри плитки; высота ряда поднята до 152dp/200dp
  - [x] 12.2. Выпилен временный debug-лог SlideShowDbg и мёртвая переменная newOrder (двойной независимый buildOrder при SHUFFLE)
  - [x] 12.3. Гонка «Продолжить» при SHUFFLE (LOW-6): при смене порядка показа позиция пересчитывается по текущему кадру (currentUri → indexOfFirst в новом order) — позиция «привязана» к кадру, а не к индексу
  - [x] 12.4. validExtension: расширения из одних цифр (photo.2023) не валидны → fallback jpg; все реальные расширения изображений буквенные
  - [x] 12.5. Сброс selectedUris при «Удалить всё» (ранее сбрасывался только deleteSelected)
  - [x] 12.6. Очистка «сиротских» *.tmp при старте ImageRepository (прерванные копии процессов)
  - [x] 12.7. Убран мёртвый state в AppUpdateHelper (логика обновления от него не зависит)
  - [x] 12.8. Выровнены отступы NavHost в SlideShowApp; debug + R8 release (lintVital) собраны
- [x] 13. (bugfix) Grid-превью «через одну» на больших наборах: в папке теста оказались служебные AppleDouble-файлы «._IMG_*.HEIC» (по одному на каждый файл — их копирует macOS). SAF их показывает, MIME по расширению = image/heic, поэтому старый фильтр «isImage» пропускал их в список, а Coil не может их декодировать (BitmapFactory → null) — половина плиток была пустой навсегда. Исправлено: isImage отсекает служебные/скрытые имена («._*», dotfiles); retainReadableUris вычищает уже добавленный мусор при входе на экран. Диагностика велась подключенным логом onError/onSuccess по плиткам (подтверждено: «._*» → ERR «BitmapFactory null», реальные HEIC декодируются, OK 0–1.5s). Debug + R8 release собраны
- [x] 14. (bugfix) Радио-кнопки в настройках переключаются по нажатию на текст: весь ряд сделан selectable (Role.RadioButton), текст и кнопка — единая зона клика
